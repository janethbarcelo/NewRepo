const username: string |number = "janeth";

const sum = (a: number, b: number) => {
  return a + b;
}
sum (2,5);

class person {
  age: number;
  lastName: string;


constructor(age: number, lastName: string){
  this.age = age;
  this.lastName = lastName;
}

  }
    const nico = new person (15, "molina");
    nico.age;
